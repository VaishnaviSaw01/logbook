apiVersion: v1
kind: Service

metadata:
  name: insights-service
  namespace: logbook

spec:
  selector:
    app: insights-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3005

  type: ClusterIP
