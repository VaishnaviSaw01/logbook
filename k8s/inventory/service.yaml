apiVersion: v1
kind: Service

metadata:
  name: inventory-service
  namespace: logbook

spec:
  selector:
    app: inventory-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3003

  type: ClusterIP