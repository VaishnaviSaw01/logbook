apiVersion: apps/v1
kind: Deployment

metadata:
  name: inventory-service
  namespace: logbook

spec:
  replicas: 1

  selector:
    matchLabels:
      app: inventory-service

  template:
    metadata:
      labels:
        app: inventory-service

    spec:
      containers:
      - name: inventory-service

        image: vaishnavisaw01/inventory-service:v2

        ports:
        - containerPort: 3003

        env:
        - name: PORT
          value: "3003"

        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: logbook-secrets
              key: MONGO_URI

        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: logbook-secrets
              key: JWT_SECRET

        livenessProbe:
          httpGet:
            path: /healthz
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 15

        readinessProbe:
          httpGet:
            path: /healthz
            port: 3003
          initialDelaySeconds: 5
          periodSeconds: 10

        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"

          limits:
            memory: "512Mi"
            cpu: "500m"
