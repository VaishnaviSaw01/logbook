apiVersion: v1
kind: Service

metadata:
  name: transaction-service
  namespace: logbook

spec:
  selector:
    app: transaction-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3004

  type: ClusterIP
  