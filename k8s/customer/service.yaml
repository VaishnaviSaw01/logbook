apiVersion: v1
kind: Service

metadata:
  name: customer-service
  namespace: logbook

spec:
  selector:
    app: customer-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3002

  type: ClusterIP